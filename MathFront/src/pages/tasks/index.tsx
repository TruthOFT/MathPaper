import { GridContent } from '@ant-design/pro-components';
import type { FC } from 'react';
import { Metric, PageHero, TaskPanel } from '@/components/MathPaper/Panels';

const Tasks: FC = () => (
  <GridContent>
    <TaskPanel />
  </GridContent>
);

export default Tasks;
